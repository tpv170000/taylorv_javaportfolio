/*
* class that encodes a tree node with integral values
 */

/**
 *
 * @author Taylor Vu 
 */
public class TreeNode {
    public int data;// a field for the integral data store in node 
    TreeNode left;// a field for the link to the left subtree
    TreeNode right;// a field for the link to thie right subtree
    public TreeNode(){ //a constructor that initalized the fields 
    	data = this.data;
    	left = null;
    	right = null;
    }
}
